# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Binaries
cmd/agent/agent
cmd/server/server
cmd/agent/agent_race
cmd/server/server_race
metricstest_v2

# Dependency directories (remove the comment below to include it)
vendor/

# IDEs directories
.idea
.vscode

# Notes
notes.txt

# Temp files
TempFileStorage
tmp_config.json*

# Certificates
*.pem

# Config files
config/*.json
!config/server.example.json
!config/agent.example.json